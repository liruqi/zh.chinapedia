# Dependencies
/node_modules

# Production
/build
# build-nowow / build-slim / build-slim2 / build-preview ...
# 注意：gitignore 不支持行尾注释，注释必须单独成行，否则整行被当成字面模式。
/build-*
/scratch
# 临时挪出去的 wow 数据（docs/wow/capycraft 是 submodule，本地调试时整体搬到这里）
/_wow_tmp

# Generated files
.docusaurus
.cache-loader
__pycache__/
*.pyc

# Misc
.DS_Store
# R2 凭证（不提交）
scripts/r2.local.json
.env.local
.env.development.local
.env.test.local
.env.production.local

npm-debug.log*
yarn-debug.log*
yarn-error.log*

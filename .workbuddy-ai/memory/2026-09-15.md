# 2026-09-15

## docs/math/孪生素数.katex.md 生成（公式 KaTeX 化）

- 以 `docs/math/孪生素数.md`（wiki2md.py 产出）为底本，只把数学公式从「行内代码 / 代码块」改成 KaTeX，
  其余文字逐字不动。产出 `docs/math/孪生素数.katex.md`（原 .md 保留）。
- 转换规模：7 个 ``` 公式块 → `$$` 独立成行的 display math；104 处行内代码 → 102 处 `$…$`。
  仅 2 处保留行内代码：孪生素数对枚举、孤立素数枚举（纯数字清单，转 math 后行内不换行会撑破布局）。
- 校验手段（可复用）：
  1. 归一化 diff：把 code span / math span 统一替换成占位符后与原文比对，确认非公式文本完全一致；
     这一步抓到了我漏抄的一个 `n`（"使得 `n`、`n + 2`…"）。
  2. katex `renderToString(..., throwOnError: true)` 逐条渲染，109/109 通过。
  3. @mdx-js/mdx compile + remark-math 解析，确认 MDX 不报错、math 节点数量正确。
- 环境：node_modules 未安装，用托管 node 的 npm 把 katex / @mdx-js/mdx / remark-math 装到
  `C:\Users\liruqi\.workbuddy-ai\binaries\node\workspace` 做校验（首次 npm install 约 3-4 分钟，需后台跑）。

## wiki2md.py 增加 --katex

- 把 SYSTEM_PROMPT 第 6 条抽成 `RULE6_CODE` / `RULE6_KATEX` 常量 + `system_prompt(katex=False)`。
- `--katex`：公式改用 `$…$` / `$$…$$`，默认输出文件名加 `.katex` 后缀，并跳过花括号 MDX 风险提示。
- 默认行为不变（仍是行内代码 / 代码块）。

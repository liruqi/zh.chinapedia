# 2026-09-23

> 当天细节已沉淀进 `notes/ref-pipeline.md`（流水线 / 多语言化 / 外链改站内）与
> `notes/ref-site.md`（构建 / 环境限制）。这里只留「做了什么、结论、提交」。

## 泰语分支完成（en.chinapedia/docs/math）

三篇译完并核对通过：`siegel_zero.md`（238 行 / 58 块 / 4m）、
`riemann_zeta_function.md`（1101 / 229 / 18m）、`riemann_hypothesis.md`（1022 / 252 / 30m）。

- 加了 `origin = git@github.com:chinapedia/math.git`（用户指定），`main` 与 `th` 都已推送并建追踪。
- 收尾抓到两类问题（已修）：**单行 chunk 的截断回复能骗过行数守卫**——`out.count("\n")+1 == n`
  对 1 行输入恒真，半截公式照样"行数对" → 新增 `dollars_balanced()` 作第二接受条件；
  **纯公式行不该送模型** → `is_math_only()` 判 verbatim。
- 校验：`check_translation.py` 三篇 0 问题；`prebuild_check.mjs` 全过；回归 39 条；
  真 MDX 管线三篇 RUNTIME OK、figure 0/6/6、812 条公式 KaTeX 报错 0。

## 词条外链改走站内链接（用户要求）

用户：「如果指向 en.wikipedia.org 的词条链接，有站内翻译好的版本，则改用站内链接」
（例 `docs/math/黎曼猜想.md:17` 的 `[黎曼ζ函数](…/Riemann_zeta_function)`）。

新增 `scripts/wikilink_localize.py` + `wikilink-aliases.json` + `test_wikilink_localize.py`（78 例）。
结果：zh 15 处、`main` 12 处、`th` 10 处（各 9/8 处 `#CITEREF*` 按设计保持外链）。
`md2zh.py` / `wiki2md.py` 已挂钩子，`prebuild_check.mjs` 加第 7 项。
**最大的坑：`slugify` 近似 github-slugger，把泰语元音/声调符号（Mn 组合字符）当标点删掉**，
锚点静默失效 → 读源码对齐 + 差分测试 303 条 0 不一致。

## 修掉构建脚本在 Windows 上全坏

`build:slim` / `build:big` / `build:faster` 都用了 POSIX 环境变量前缀，而 npm 在 Windows 上用
cmd.exe 跑 scripts → `'SKIP_WOW' is not recognized as an internal or external command`。
报错在 npm 层、看着像环境问题，所以一直没被发现。新增 `scripts/build.mjs`（Node 设环境变量
再 spawn docusaurus），四个脚本改走它；`DEPLOY.md` 同步。
顺带确认本环境 safe-delete shim 会拦对已存在 `build-slim/` 的批量删除（宿主限制，非脚本问题）。

## 校验

`npm run check` 18 篇全过；`build:slim` 成功、**0 条 broken link / broken anchor 警告**；
产物里 `en.wikipedia.org/wiki/Riemann_zeta_function` 计数 **0**，链接已变成 `/wiki/math/黎曼ζ函数` 等。

## 提交

- zh.chinapedia：`eab0cb0` 外链改站内 → `ad37ca3` slugify → `329e013` 记忆 →
  `e399f5c` 构建脚本 → `072f6a2` 记忆
- chinapedia/math：`main` `cbb1725`、`th` `7df58cf`（均已推送）

## 记忆维护

`memory/` 注入量已超预算（MEMORY.md 3564 字 > 3000 上限，全目录 14955 字节）→ 收敛到
MEMORY.md 2996 字、全目录 7799 字节。细节本就已在 `notes/ref-*.md`；顺带把两条只存在于
日志里的事实补进 notes（`rm` 的 `;` 陷阱、`孪生素数.md` 被外部还原）。

## 未解

`docs/math/孪生素数.md` 改写后于 18:05:41 被外部还原成 HEAD（无 prebuild 钩子、无 stash、
reflog 无变化），已重做并提交，之后未再复现 → **再出现先怀疑编辑器 / 同步工具**。

# 2026-09-23

## 泰语分支完成（en.chinapedia/docs/math）

三篇稿子全部译完并核对通过，`th` 分支提交 `2caeb0c`：

| 文件 | 行数 | 块数 | 耗时 | 标题 |
|---|---|---|---|---|
| `siegel_zero.md` | 238 | 58 | 4m | `ศูนย์ของซีเกิล` |
| `riemann_zeta_function.md` | 1101 | 229 | 18m | `ฟังก์ชันซีตาของรีมัน` |
| `riemann_hypothesis.md` | 1022 | 252 | 30m | `สมมติฐานของรีมันน์` |

`main`（英文）`18683ea`，`th`（泰语）`2caeb0c`；`git diff main..th` 正常。

### 收尾时又抓到两类问题（都已修）

1. **单行 chunk 的截断回复能骗过行数守卫**。`out.count("\n")+1 == n` 对 1 行输入恒真，
   所以 110 / 112 号 chunk 回来是半截（`$\Pi_0(x) = … li}(x^\`），行数照样"对"。
   → 新增 `dollars_balanced()` 作为第二个接受条件（逐行比 `$` 奇偶），不通过就重试；
   `translate_line()` 兜底两次仍不配对就退回原文。
   修法：把这两个坏块从 state 里删掉重跑（2 次请求 / 90s）就干净了。
2. **纯公式行不该送模型**：`  $\Pi_0(x) = … \log t}$,` 这种整行公式本来没内容可译，
   送进去只会被"译"坏 → `is_math_only()` 判 verbatim。

### 最终校验

- `scripts/check_translation.py`：三篇全 0 问题（行数 / `$$` / `$` 逐行配对 / 脚注标记与
  定义 / figure 与 figcaption / 表格行数 / 相邻脚注间距 / 残留 / 应译行是否真译）
- `scripts/prebuild_check.mjs`：三篇全过
- 回归用例 39 条（`scripts/test_fix_footnotes.py`）
- **真 MDX 管线**（补跑的，之前只过了静态检查）：
  - `scratch/_mdxrun.mjs`：三篇 RUNTIME OK，无 `ReferenceError`
  - `scratch/_figcheck.mjs`：figure / figcaption / img = 0/6/6，与英文原稿一致
  - `scratch/_macroscan.mjs`：812 条公式，KaTeX 报错 0
  → 结论：三篇泰语稿可以直接进 Docusaurus 渲染，不需要额外改动

### 提交（zh.chinapedia）

`532cc8b` 记忆收敛 → `0bf4c88` 多语言化+4 个修复 → `020ac92` 标题裸尖括号 →
`1955552` ζ 稿 3 个问题 → `54352b3` 截断守卫。共 5 个待推送提交。
